## Infrastructure / Compose

```txt
01. Rename DB port variables
02. Fix Postgres host port
03. Split host DB config and Docker DB config
04. Create shared Docker network
05. Keep Vault internal network private
06. Add Vault HAProxy
07. Fix Vault api_addr / cluster_addr
08. Fix Vault volume env names
09. Make Vault init scripts restart-safe
10. Add root dev compose later
```

## Vault / Secrets

```txt
11. Enable Vault Transit engine
12. Create JWT signing Transit key
13. Disable Transit key export
14. Add Vault policy for auth service
15. Add Vault token/config for auth service
16. Add KV engine for normal app secrets
17. Store DB password in Vault KV later
18. Store refresh-token pepper in Vault KV
19. Add Vault health checks
20. Add Vault HAProxy health routing
```

## Auth Key Architecture

```txt
21. Create signing_keys table
22. Remove private_key_pem from DB
23. Store Vault key reference in DB
24. Store public key metadata in DB
25. Add active/retired key status
26. Add key rotation model
27. Add JWKS endpoint
28. Add JWKS cache in gateway
29. Add local JWT verification
30. Avoid Vault call per request
```

## Alembic / ORM

```txt
31. Create src package layout
32. Add SQLAlchemy Base
33. Add database URL builder
34. Add session factory
35. Add models directory
36. Add SigningKey ORM model
37. Wire Alembic target_metadata
38. Import models in Alembic env.py
39. Add Alembic autogeneration
40. Review autogenerated migrations manually
```

## Database / Migrations

```txt
41. Create users table
42. Create user_credentials table
43. Create sessions table
44. Create refresh_tokens table
45. Create signing_keys table
46. Create security_events table
47. Add created_at / updated_at columns
48. Add indexes for login fields
49. Add token hash indexes
50. Add migration review rule
```

## Auth Service

```txt
51. Create auth service skeleton
52. Add config loader
53. Add Postgres connection
54. Add Vault client
55. Add password hashing
56. Add register endpoint
57. Add login endpoint
58. Add refresh endpoint
59. Add logout endpoint
60. Add revoke session endpoint
```

## JWT / Token Flow

```txt
61. Define JWT claims
62. Add access token issuer
63. Sign JWT through Vault Transit
64. Add kid to JWT header
65. Add short access token TTL
66. Add refresh token generation
67. Store refresh token hash only
68. Add refresh token rotation
69. Detect refresh token reuse
70. Add token revocation logic
```

## Gateway

```txt
71. Create gateway skeleton
72. Add JWKS fetcher
73. Add JWKS cache
74. Add JWT middleware
75. Verify issuer/audience/expiry
76. Strip client identity headers
77. Inject trusted identity headers
78. Route to auth service
79. Route to messenger service later
80. Add gateway auth error format
```

## Security / Cleanup

```txt
81. Rename .env to .env.example
82. Remove real secrets from repo
83. Add .gitignore for local secrets
84. Add least-privilege Vault policies
85. Add least-privilege DB user
86. Add password hashing parameters
87. Add audit/security events
88. Add Docker health checks
89. Add local dev README
90. Add architecture notes
```

My first concrete batch would be:

```txt
01. Fix compose/env naming
02. Fix Vault addresses
03. Fix Vault volume env names
04. Create ORM directory layout
05. Wire Alembic to SQLAlchemy metadata
06. Create signing_keys ORM/table without private_key_pem
07. Enable Vault Transit engine
08. Create auth-jwt Transit key
09. Add JWKS endpoint design
10. Add local JWT verification design
```

This gives you a clean base before you start writing actual login/register logic.
